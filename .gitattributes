# По умолчанию текстовые файлы — LF
* text=auto eol=lf

# Bash-скрипты — строго LF (критично для Linux)
*.sh text eol=lf

# Конфиги для Linux-серверов — LF
*.tmpl text eol=lf
*.service text eol=lf

# Бинарные файлы — как есть
*.png binary
*.ico binary
*.gz binary
