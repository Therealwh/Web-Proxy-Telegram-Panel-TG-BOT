# План v1.3.3 — оплата: выбор CryptoBot/карта, отмена платежей, username в панели

## Диагноз (по скринам)
1. Buy-флоу: платёжка настроена, но инвойс создаётся при нажатии сразу и
   падает (pay.crypt.bot часто недоступен с RU VPS) → юзеру остаётся только
   админ. НУЖНО: выбор способа СНАЧАЛА, инвойс — лениво по клику.
2. Панель: ожидающие платежи — добавить «Отменить» (статус cancelled).
3. Панель: рядом с TG ID показывать username клиента, если есть.

## Задачи
- [ ] bot.js buy: выбор способа (баланс/CryptoBot/ЮKassa/карта админа),
      инвойс создаётся лениво в cbpay:<pid>:<provider>, ошибки — юзеру текстом
- [ ] payments.js: экспорт createCryptoBotInvoice/createYooKassaPayment
- [ ] routes/payments.js: GET /payments → username; POST /:id/cancel
- [ ] Dashboard.jsx: кнопка «Отменить» + username в строке
- [ ] Проверка: тест один раз с аккаунта (без изменений)
- [ ] VERSION 1.3.3, CHANGELOG, сборка, тесты, пуш, тег, релиз
